{
  "name": "wap-final-project",
  "version": "1.0.0",
  "description": "Amazing Music Player Project",
  "main": "app.js",
  "scripts": {
    "start": "set PORT=3000 && set NODE_ENV=development && nodemon app.js",
    "dev": "set PORT=3000 && set NODE_ENV=development && nodemon app.js",
    "production": "set PORT=3000 && set NODE_ENV=production && nodemon app.js"
  },
  "author": "TDM",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "moment": "^2.30.1",
    "nodemon": "^3.1.0"
  }
}
